int ingresarNumero (float* numero);
/** \brief  Asigna un numero a los flotantes numero1 y numero2
 *
 * \param Se pide el ingreso de un número.
 * \param Limpiar pantalla.
 * \return Se retorna un numero que será asignado a numero1 y numero2.
 *
 */

int sumar (float numero1, float numero2);
/** \brief la suma de dos numeros
 *
 * \param Variable suma con 0 asignado.
 * \param Se suma el valor numero1 y numero2.
 * \return Retorna el resultado de la suma.
 *
 */

int restar (float numero1,float numero2);
/** \brief Se utiliza para la resta de dos numero, que están asignados en numero1 y numero2
 *
 * \param Variable resta con valor 0.
 * \param Se restan ambos numeros
 * \return Se devuelve la suma de los numeros asignados
 *
 */

double dividir (float numero1, float numero2);
/** \brief Sirve para dividir dos numeros
 *
 * \param Se asigna 0 y se crea la variable division.
 * \param Pregunta si numero 2 es igual a cero, de ser así devuelve un mensaje
 * \param En caso de que numero2 sea distinto a 0 se realiza la division
 * \return Devolvemos el resultado
 *
 */

int multiplicar (float numero1, float numero2);
/** \brief Esta funcion multiplicará dos numeros
 *
 * \param Se crea la variable multiplicacion y se le asigna valor 0.
 * \param Se hace la multiplicacion de ambos numeros 1 y 2
 * \return Devuelve resultado.
 *
 */

double factorizar (float numero);
/** \brief Para factorizar dos numeros que será asignados a numero1 y numero2
 *
 * \param se crea la variable factorial y se le asigna 0;
 * \param se pregunta si igual numero es igual a 1
 * \param se reotrna 1.
 * \param se realiza el calculo de los factoriales A y B

 * \return Se retorna el resultado de ambas
 *
 */

